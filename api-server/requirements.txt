# ═══════════════════════════════════════════════════════════════════════════
# AutoCommerce V25 — Production Requirements (Python 3.12)
# Fixed: starlette pin relaxed, bcrypt locked to 4.0.1 (passlib compat)
# ═══════════════════════════════════════════════════════════════════════════

# ─── Web framework ─────────────────────────────────────────────────────────
fastapi==0.133.1
uvicorn[standard]==0.34.3
# starlette pinned to the patched 1.x line to clear the 2026 advisory set.
# fastapi 0.133.1 is the first compatible branch here without a starlette <1.0 cap.
starlette==1.3.1
python-multipart==0.0.31

# ─── ORM / DB ──────────────────────────────────────────────────────────────
SQLAlchemy[asyncio]==2.0.41
alembic==1.14.0
asyncpg==0.30.0
psycopg2-binary==2.9.10

# ─── Pydantic / Settings ───────────────────────────────────────────────────
pydantic==2.11.4
pydantic-settings==2.6.1
email-validator==2.2.0

# ─── Auth / Crypto ─────────────────────────────────────────────────────────
# CRITICAL: bcrypt must stay at 4.0.1 — bcrypt>=4.1 removes __about__.__version__
# which passlib 1.7.4 reads internally, causing AttributeError at startup.
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
PyJWT==2.13.0
cryptography==48.0.1
pyotp==2.9.0

# ─── HTTP / Networking ─────────────────────────────────────────────────────
httpx==0.28.1
requests==2.33.0

# ─── Queue asynchrone ──────────────────────────────────────────────────────
celery==5.5.2
redis==5.2.0  # SKIP: redis 6.x major — breaking async API; upgrade separately

# ─── Rate limiting ─────────────────────────────────────────────────────────
slowapi==0.1.9

# ─── Logging / Observability ───────────────────────────────────────────────
structlog==24.4.0
opentelemetry-api==1.28.2
opentelemetry-sdk==1.28.2
opentelemetry-exporter-otlp-proto-grpc==1.28.2
opentelemetry-instrumentation-fastapi==0.49b2
opentelemetry-instrumentation-sqlalchemy==0.49b2

# ─── Métriques (optionnel) ─────────────────────────────────────────────────
prometheus_client==0.21.0
prometheus_fastapi_instrumentator==8.0.2  # Required for /metrics endpoint in main.py

# ─── Error tracking (optionnel) ────────────────────────────────────────────
sentry-sdk[fastapi]>=2.0.0

# ─── AI / LLM ──────────────────────────────────────────────────────────────
openai==1.86.0

# ─── Paiements ─────────────────────────────────────────────────────────────
stripe==11.3.0  # SKIP: stripe 12.x major — breaking webhook API; upgrade separately

# ─── PDF / Imaging / QR ────────────────────────────────────────────────────
reportlab==4.2.5
qrcode[pil]==8.0
Pillow==12.2.0

# ─── Object Storage S3/MinIO ───────────────────────────────────────────────
boto3==1.38.28
botocore==1.38.28

# ─── Testing ───────────────────────────────────────────────────────────────
pytest==9.0.3
pytest-asyncio==1.4.0
anyio==4.6.2.post1
aiosqlite==0.20.0
pytest-cov==5.0.0
factory-boy==3.3.0
respx==0.21.1
pytest-timeout==2.3.1
ruff==0.11.12
